# 专业支持、多语言和移动端发布包

用户授权发布当前本地版本，基线生产提交为 `9c4a7fe`。

范围：专业支持三页的设计、文案及五张自托管配图；复合技术内容；简体中文、繁体中文和 English 语言入口及 CMS 译文；手机布局；统一轮播控件；应用关联系列入口；Footer 第一层重排和桌面二维码悬停。保留已确认的 Footer 第二层及 CMS 齿轮，备案只显示简体中文。

内容清单为 `files/releases/support-content-20260922.json`，22 个字段或公开内容集合更新。发布前读取生产基线，每项检查 before/after；CMS 出现并发修改即中止。仅四个支持内容集合允许整体替换，其他内容按字段修改；账号、询盘、邮件配置不进入清单。语言菜单和缺失翻译使用一次性、幂等迁移。

沿用已配置的内容发布连接及已固定的服务器指纹。发布过程锁定任务，备份数据库、环境文件及构建，快进到准确提交，安装锁定依赖、构建、停止单实例 CMS 后再次核对并写入内容，再启动并检查健康接口提交号。发布日志和准确备份位置保留在服务器 logs/backups 与本地忽略目录；不提交凭据。

验收包括发布检查、内容冲突与私有数据保留测试、线上健康提交号、中英文专业支持、Footer 和二维码入口。发布完成状态以对应提交的线上健康接口为准。
