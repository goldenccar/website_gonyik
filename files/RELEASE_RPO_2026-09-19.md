# RPO-Tech 生产发布记录

2026-09-19。用户确认将当前审阅版发布生产，字体保持现状。

- 原生产版本：`462ca420ca3373dadf61b598bca46144fae906ad`。
- 本次生产版本：`52f5b87de551bc2621f4c507a3904485ecf83a61`，已推送 GitHub main。
- 范围：六个技术页面、统一 Mega Menu、菜单切换修复、相应 CMS 编辑能力、纤维配图及旧组件清理。
- 尚未改动：OTTER / RAYO / KAIS 系列详情页及产品应用页。本轮对这些页面只做研究。

## 内容迁移与数据保留

使用 `files/releases/rpo-content-20260919.json` 保存审阅前后公开字段差异，以 `scripts/publish-rpo-content.ts` 应用。共 66 个字段，限定为首页平台入口文案、导航和技术内容。迁移前检查生产字段仍与审阅基线一致，遇到新的 CMS 修改会停止。未用本地公开镜像覆盖完整生产数据库。

代码、前端构建与数据库均已备份。服务器数据库备份相对路径：`backups/db-20260919T123224Z-52f5b87de551bc2621f4c507a3904485ecf83a61.json`。原数据库字段完整比较通过；媒体库另自动登记了纤维新图与测试页配图，原有媒体记录保持不变。

沿用既有本机凭据和固定主机密钥的 SSH 发布连接，执行已提交版本的快进更新、前端构建、停止单实例 CMS、定向内容迁移与服务重启。此版本需要单独的 CMS 迁移，未直接运行不包含该步骤的通用 `npm run deploy`。

发布时修正了 `.env.production` 中过时的部署版本标记；其他环境字段未改动。Windows 发布输出编码问题已处理，最终状态通过远程与公网检查确认，不以中途输出作为成功依据。

## 验证

- 23 项测试通过，包括迁移不覆盖其他资料、重复执行无副作用、遇到后续 CMS 编辑拒绝覆盖。
- 类型检查、构建、质量审计通过。
- 公网 `/api/health` 返回本次准确版本。
- 公网导航与六个技术页面关键字段逐项匹配本地审阅稿。
- 首页、平台概览、测试页、后台入口与新配图均返回 HTTP 200。
- 浏览器看到新的 RPO-Tech 导航和精简后的测试页。

后续恢复应先保全恢复时的新数据，再按本记录定位代码和内容差异；不要直接用旧备份覆盖新询盘或后台修改。
