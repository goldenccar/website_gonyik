# 应用场景、导航与系列入口发布

用户明确授权发布当前本地审阅版本。此前生产提交 `a0c355c`。

发布包含应用分类与六张场景图手动循环、全幅栏目头图、统一次级导航、Mega Menu 精简、面料目录卡片与数据维护、系列新版配图，以及 RPO 概览共用 CMS 系列资料。

内容清单：`files/releases/applications-content-20260920.json`，121 项字段更新或新增记录。服务器端已只读验证基线无冲突。复用 `scripts/publish-rpo-content.ts` 进行定向写入；冲突即中止，重复应用不改后续资料。不使用本地数据库整体覆盖线上。

发布过程沿用既有内容发布连接，执行锁定、代码快进、前端构建、停服务后再次备份及冲突检查、应用清单、重启和准确提交号健康检查。线上账号、密码哈希、询盘及无关数据保持不变。CMS 生产用户名为 admin，原密码不可从哈希恢复；此次不重置密码。

本地生成素材为应用场景示意，未将其标注为客户实拍或合作案例。多媒体地址、替代文本与顺序由 CMS 管理。
